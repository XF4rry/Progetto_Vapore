<!DOCTYPE html>
<html lang="it" dir="ltr">

<style>

  body{
    background-color: transparent;
        /* Aggiungo l'immagine di sfondo */
        background-image: url('https://lh3.googleusercontent.com/proxy/9ktZ0MJPNk_h42EGen2pblARoY_6aAerX44magNafJFnxKPth8edVo07rSdYO2awTNhD5lzncBosgFq8SRihwIxIYLirRW3LVrk6oXgbIVncwOvSrstjdSRh');
        /* Specifico il modo in cui l'immagine di sfondo deve essere ripetuta */
        background-repeat: repeat; /* o repeat-x, repeat-y, no-repeat per non ripetere */
        /* Imposto il posizionamento dell'immagine di sfondo */
        background-position: center center;
        /* Imposto la dimensione dell'immagine di sfondo */
        background-size: cover; /* o contain per adattare l'immagine all'area del body */
        /* Imposto un fallback color in caso l'immagine non sia disponibile */
        background-color: #f3f3f3; /* Cambia con il colore desiderato */
  }
    .card {
  width: 350px;
  height: 450px;
  background: #313131;
  border-radius: 20px;
  ;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: white;
  transition: 0.2s ease-in-out;
  margin: 10px;
  
}

.img {
    width: 345px;
  height: 30%;
  position: absolute;
  transition: 0.2s ease-in-out;
  object-fit: cover;
}

.textBox {
  opacity: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 15px;
  transition: 0.2s ease-in-out;
  z-index: 2;
}

.textBox > .text {
  font-weight: bold;
}

.textBox > .head {
  font-size: 20px;
}

.textBox > .price {
  font-size: 17px;
}

.textBox > span {
  font-size: 12px;
  color: lightgrey;
}

.card:hover > .textBox {
  opacity: 1;
}
.wrapper{
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    
}
.card:hover > .img {
  height: 65%;
  filter: blur(7px);
  animation: anim 3s infinite;
}

@keyframes anim {
  0% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(-20px);
  }

  100% {
    transform: translateY(0);
  }
}

.card:hover {
  transform: scale(1.04);
}


</style>

<head>
    <meta charset="utf-8">
    <title>Shop</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
    <header class="bg-gray-900 text-white text-center py-4">
        <h1 class="text-2xl">Benvenuti nel nostro negozio online</h1>
    </header>
    <nav class="bg-gray-800 text-white text-center py-2">
        <a href="/" class="hover:underline">Home</a>
        <a href="/carrello" class="hover:underline">Carrello</a>
        <a href="/bilancio" class="hover:underline">Profilo</a>
        
    </nav>
    <section class="container mx-auto px-4 py-8">
        
        
            
            
              <div class="wrapper">
            <% lista.forEach((element) => { %>
              <form action="" method="post">
                <div class="card">
                    <div class="img">
                        <img src="<%= element.thumbnail %>"  width="100%" height="100%">
                    </img>
                 </div>
                    <div class="textBox">
                        <p id="titolo" class="text head"><%= element.title %></p>
                        <span><%= element.genre %></span>
                        <p class="text price"><%= element.short_description %></p>
                        <p class="text price"><%= element.developer %></p>
                        
                        <button type="submit" id="titolo" name="titolo" value="<%= element.title %>" class="btn bg-red-500 hover:bg-gray-600 text-white font-semibold py-2 px-4 rounded-full shadow-md transition duration-300">Aggiungi</button>

                    </div>
                </div>
              </form>
            <% }) %>
              </div>
            
            

        
    </section>
    
</body>

</html>

<!DOCTYPE html>
<html lang="it" dir="ltr">

<head>
    <meta charset="utf-8">
    <title>Shop</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
         .card {
  width: 350px;
  height: 450px;
  background: #313131;
  border-radius: 20px;

  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: white;
  transition: 0.2s ease-in-out;
  margin: 10px;
  
}
body{
    background-color: transparent;
        /* Aggiungo l'immagine di sfondo */
        background-image: url('https://www.sapere.it/.imaging/default/dam/icone-sapere/domande-risposte/scienza-tecnologia/vapore/jcr:content.jpg');
        /* Specifico il modo in cui l'immagine di sfondo deve essere ripetuta */
        background-repeat: repeat; /* o repeat-x, repeat-y, no-repeat per non ripetere */
        /* Imposto il posizionamento dell'immagine di sfondo */
        background-position: center center;
        /* Imposto la dimensione dell'immagine di sfondo */
        background-size: cover; /* o contain per adattare l'immagine all'area del body */
        /* Imposto un fallback color in caso l'immagine non sia disponibile */
        background-color: #f3f3f3; /* Cambia con il colore desiderato */
  }
.img {
    width: 345px;
  height: 30%;
  position: absolute;
  transition: 0.2s ease-in-out;
  object-fit: cover;
}

.textBox {
  opacity: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 15px;
  transition: 0.2s ease-in-out;
  z-index: 2;
}

.textBox > .text {
  font-weight: bold;
}

.textBox > .head {
  font-size: 20px;
}

.textBox > .price {
  font-size: 17px;
}

.textBox > span {
  font-size: 12px;
  color: lightgrey;
}

.card:hover > .textBox {
  opacity: 1;
}
.wrapper{
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    
}
.card:hover > .img {
  height: 65%;
  filter: blur(7px);
  animation: anim 3s infinite;
}

@keyframes anim {
  0% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(-20px);
  }

  100% {
    transform: translateY(0);
  }
}

.card:hover {
  transform: scale(1.04);
}

    </style>
</head>

<body class="bg-gray-100">
    <header class="bg-gray-900 text-white text-center py-4">
        <h1 class="text-2xl">Benvenuti nel nostro negozio online</h1>
    </header>
    <nav class="bg-gray-800 text-white text-center py-2">
        <a href="/" class="hover:underline">Home</a>
        <a href="/shop" class="hover:underline ml-2">Shop</a>
        <a href="/bilancio" class="hover:underline ml-2">Profilo</a>
    </nav>
    <section class="container mx-auto px-4 py-8">
        
            <div class="wrapper">
                <% lista.forEach((element) => { %>
                <% for (i = 0; i < titoli.length; i++) { %>
                <% if (titoli[i] == element.title) { %>
                    <form action="#" method="post">
                        <div class="card">
                            <div class="img">
                                <img src="<%= element.thumbnail %>"  width="100%" height="100%">
                            </div>
                            <div class="textBox">
                                <p id="titolo" class="text head"><%= element.title %></p>
                                <span><%= element.genre %></span>
                                <p class="text price"><%= element.short_description %></p>
                                <p class="text price"><%= element.developer %></p>
                                
                                

                              
                            </div>
                        </div>
                    </form>
                <% } %>
                </form>
                <% } %>
                <% }) %>
            </div>
        
        <div class="flex flex-col items-center justify-center">
          <p class="text-center text-lg font-semibold mt-4 
                      {{ $pagamento === 'Avvenuto' ? 'text-green-500' : 'text-red-500' }}">
              <%= pagamento %>
          </p>
          <form action="" method="post">
              <div class="text-center mt-4"> <!-- Aggiunta della classe text-center -->
                  <button type="submit" id="pulsante" class="btn bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded-full shadow-md transition duration-300" value="paga">Conferma Pagamento</button>
              </div>
          </form>
      </div>
      

    </section>
   

   
</body>

</html>
